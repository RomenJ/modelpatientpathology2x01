<form method="post" action="{{ path('app_diagnosis_delete', {'id': diagnosi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ diagnosi.id) }}">
    <button class="btn">Delete</button>
</form>
