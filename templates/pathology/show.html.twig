{% extends 'base.html.twig' %}

{% block title %}Pathology{% endblock %}

{% block body %}
    <h1>Pathology</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pathology.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pathology.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pathology_index') }}">back to list</a>

    <a href="{{ path('app_pathology_edit', {'id': pathology.id}) }}">edit</a>

    {{ include('pathology/_delete_form.html.twig') }}
{% endblock %}
